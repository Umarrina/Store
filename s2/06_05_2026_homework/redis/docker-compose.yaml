services:
  redis:
    image: redis:7
    container_name: ${REDIS_CONTAINER:-redis-lab}
    ports:
      - "${REDIS_PORT:-6379}:6379"
    command: redis-server --appendonly yes
    volumes:
      - redis-data:/data
volumes:
  redis-data: